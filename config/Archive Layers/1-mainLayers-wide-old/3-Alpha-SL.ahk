#HotIf currentLayer = "Alpha-Sl"
; ====================================== LETTERS ======================================
*q::{
        if(A_PriorKey != "q" || A_TimeSincePriorHotkey > 500){
            longPress("q", "{Blind}W", "{Blind}w", 1)
        } else {
            SendInput("{Blind}W")
        }
    }
        *w::{
        if(A_PriorKey != "w" || A_TimeSincePriorHotkey > 500){
            longPress("w", "{Blind}L", "{Blind}l", 1)
        } else {
            SendInput("{Blind}L")
        }
    }
        *e::{
        if(A_PriorKey != "e" || A_TimeSincePriorHotkey > 500){
            longPress("e", "{Blind}Y", "{Blind}y", 1)
        } else {
            SendInput("{Blind}Y")
        }
    }
        *r::{
        if(A_PriorKey != "r" || A_TimeSincePriorHotkey > 500){
            longPress("r", "{Blind}P", "{Blind}p", 1)
        } else {
            SendInput("{Blind}P")
        }
    }
        *t::{
        if(A_PriorKey != "t" || A_TimeSincePriorHotkey > 500){
            longPress("t", "{Blind}K", "{Blind}k", 1)
        } else {
            SendInput("{Blind}K")
        }
    }
        *y::{
        if(A_PriorKey != "y" || A_TimeSincePriorHotkey > 500){
            longPress("y", "{Blind}y", "{Blind}y", 1)
        } else {
            SendInput("{Blind}y")
        }
    }
        *u::{
        if(A_PriorKey != "u" || A_TimeSincePriorHotkey > 500){
            longPress("u", "{Blind}Z", "{Blind}z", 1)
        } else {
            SendInput("{Blind}Z")
        }
    }
        *i::{
        if(A_PriorKey != "i" || A_TimeSincePriorHotkey > 500){
            longPress("i", "{Blind}X", "{Blind}x", 1)
        } else {
            SendInput("{Blind}X")
        }
    }
        *o::{
        if(A_PriorKey != "o" || A_TimeSincePriorHotkey > 500){
            longPress("o", "{Blind}O", "{Blind}o", 1)
        } else {
            SendInput("{Blind}O")
        }
    }
        *p::{
        if(A_PriorKey != "p" || A_TimeSincePriorHotkey > 500){
            longPress("p", "{Blind}U", "{Blind}u", 1)
        } else {
            SendInput("{Blind}U")
        }
    }
        *a::{
        if(A_PriorKey != "a" || A_TimeSincePriorHotkey > 500){
            longPress("a", "{Blind}C", "{Blind}c", 1)
        } else {
            SendInput("{Blind}C")
        }
    }
        *s::{
        if(A_PriorKey != "s" || A_TimeSincePriorHotkey > 500){
            longPress("s", "{Blind}R", "{Blind}r", 1)
        } else {
            SendInput("{Blind}R")
        }
    }
        *d::{
        if(A_PriorKey != "d" || A_TimeSincePriorHotkey > 500){
            longPress("d", "{Blind}S", "{Blind}s", 1)
        } else {
            SendInput("{Blind}S")
        }
    }
        *f::{
        if(A_PriorKey != "f" || A_TimeSincePriorHotkey > 500){
            longPress("f", "{Blind}T", "{Blind}t", 1)
        } else {
            SendInput("{Blind}T")
        }
    }
        *g::{
        if(A_PriorKey != "g" || A_TimeSincePriorHotkey > 500){
            longPress("g", "{Blind}B", "{Blind}b", 1)
        } else {
            SendInput("{Blind}B")
        }
    }
        *h::{
        if(A_PriorKey != "h" || A_TimeSincePriorHotkey > 500){
            longPress("h", "{Blind}h", "{Blind}h", 1)
        } else {
            SendInput("{Blind}h")
        }
    }
        *j::{
        if(A_PriorKey != "j" || A_TimeSincePriorHotkey > 500){
            longPress("j", "{Blind}F", "{Blind}f", 1)
        } else {
            SendInput("{Blind}F")
        }
    }
        *k::{
        if(A_PriorKey != "k" || A_TimeSincePriorHotkey > 500){
            longPress("k", "{Blind}N", "{Blind}n", 1)
        } else {
            SendInput("{Blind}N")
        }
    }
        *l::{
        if(A_PriorKey != "l" || A_TimeSincePriorHotkey > 500){
            longPress("l", "{Blind}E", "{Blind}e", 1)
        } else {
            SendInput("{Blind}E")
        }
    }
        *;::{
        if(A_PriorKey != ";" || A_TimeSincePriorHotkey > 500){
            longPress(";", "{Blind}I", "{Blind}i", 1)
        } else {
            SendInput("{Blind}I")
        }
    }
        *'::{
        if(A_PriorKey != "'" || A_TimeSincePriorHotkey > 500){
            longPress("'", "{Blind}A", "{Blind}a", 1)
        } else {
            SendInput("{Blind}A")
        }
    }
        *z::{
        if(A_PriorKey != "z" || A_TimeSincePriorHotkey > 500){
            longPress("z", "{Blind}J", "{Blind}j", 1)
        } else {
            SendInput("{Blind}J")
        }
    }
        *x::{
        if(A_PriorKey != "x" || A_TimeSincePriorHotkey > 500){
            longPress("x", "{Blind}V", "{Blind}v", 1)
        } else {
            SendInput("{Blind}V")
        }
    }
        *c::{
        if(A_PriorKey != "c" || A_TimeSincePriorHotkey > 500){
            longPress("c", "{Blind}D", "{Blind}d", 1)
        } else {
            SendInput("{Blind}D")
        }
    }
        *v::{
        if(A_PriorKey != "v" || A_TimeSincePriorHotkey > 500){
            longPress("v", "{Blind}G", "{Blind}g", 1)
        } else {
            SendInput("{Blind}G")
        }
    }
        *b::{
        if(A_PriorKey != "b" || A_TimeSincePriorHotkey > 500){
            longPress("b", "{Blind}Q", "{Blind}q", 1)
        } else {
            SendInput("{Blind}Q")
        }
    }
        *n::{
        if(A_PriorKey != "n" || A_TimeSincePriorHotkey > 500){
            longPress("n", "{Blind}n", "{Blind}n", 1)
        } else {
            SendInput("{Blind}n")
        }
    }
        *m::{
        if(A_PriorKey != "m" || A_TimeSincePriorHotkey > 500){
            longPress("m", "{Blind}M", "{Blind}m", 1)
        } else {
            SendInput("{Blind}M")
        }
    }
        *,::{
        if(A_PriorKey != "," || A_TimeSincePriorHotkey > 500){
            longPress(",", "{Blind}H", "{Blind}h", 1)
        } else {
            SendInput("{Blind}H")
        }
    }
        
; ====================================== ADDITIIONAL KEYS ======================================
CapsLock::{
        toggleLayer("Ext")
    }
RAlt::toggleLayer("Sym-D")
.::toggleLayer("Alpha")
*LAlt::{
    SendInput("{Alt downR}")
    if !(released := KeyWait("LAlt", "T0.22")){
        KeyWait("LAlt")
    }
    SendInput("{Alt up}")
    if(released && ThisHotkey = A_ThisHotkey) {
        SendInput("{Alt downR}{Alt up}")
        toggleLayer("Directory-I")
    }
}
*PrintScreen::{
    SendInput("{RControl downR}")
    if !(released := KeyWait("PrintScreen", "T0.22")){
        KeyWait("PrintScreen")
    }
    SendInput("{RControl up}")
    if(released && ThisHotkey = A_ThisHotkey) {
        toggleLayer("Sym")
    }
}
*Control::{
    SendInput("{RControl downR}")
    if !(released := KeyWait("Control", "T0.22")){
        KeyWait("Control")
    }
    SendInput("{RControl up}")
    if(released && ThisHotkey = A_ThisHotkey) {
        toggleLayer("Sym")
    }
}
/::{
    if !(released := KeyWait("/", "T0.22")){
        SendInput("{RWin downR}")
        KeyWait("/")
    }
    SendInput("{RWin up}")
    if(released && ThisHotkey = A_ThisHotkey) {
        SendInput("^{Backspace}")
    }
}
    
    